<% @garden.plots.each do |plot| %>
  <u><h2>Plot Number: <%= plot.number %> </h2></u>
  <p><%plot.plants.each do |p|%>
    <%= p.name %> <%= link_to "Delete Plant", garden_plots_path(@garden, plot), method: :delete %>
  <%end%>
<% end %>